import { STYLING, COLORS } from '../../config/constants';

export const styles = {
  content: {
    ...STYLING.flex,
    marginTop: 20,
  },
  headline: {
    marginHorizontal: 18,
    marginBottom: 15,
    color: COLORS.offGrey
  }
};